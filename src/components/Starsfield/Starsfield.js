import React from 'react';
import { Starfield } from './styled';

const Starsfield = () => {
  // const spaceRef = useRef(null);
  // const startRef = useRef(null);
  // const limit = 1001;

  // console.log(spaceRef, 'spaceRef');

  // for (let i = 0; i <= limit; i++) {
  //   startRef.style.top = `${Math.random() * 100}%`;
  //   startRef.style.left = `${Math.random() * 100}%`;
  //   startRef.style.animationDelay = `${Math.random() * 8}s`;
  // }

  return (
    <Starfield />
  )
};

export default Starsfield;

// https://codepen.io/faither/pen/PoeNyyy
